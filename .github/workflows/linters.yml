#################################
#################################
## Super Linter GitHub Actions ##
#################################
#################################
name: Lint Code Base

on:
  push:
    branches-ignore: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    name: Lint Code Base
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      ##########################
      # Checkout the code base #
      ##########################
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # CRITICAL: Ensures the check out git history is explicitly kept in place
          persist-credentials: false

      ###########################################################
      # Fix Git ownership issues to safely accept cloned repos  #
      ###########################################################
      - name: Add workspace to git safe directories
        run: |
          git config --global --add safe.directory /github/workspace
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      ################################
      # Run Linter against code base #
      ################################
      - name: Lint Code Base
        uses: super-linter/super-linter@v7
        env:
          # Change to true if it still hangs on a complex PR branch
          VALIDATE_ALL_CODEBASE: false
          DEFAULT_BRANCH: main
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VALIDATE_JSCPD: false
          FILTER_REGEX_EXCLUDE: ".*tests/.*"
